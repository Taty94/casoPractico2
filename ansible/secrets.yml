---
# Azure Container Registry credentials
# ACR Username - Desde terraform output acr_username
acr_username: "{{ lookup('file', '../terraform/outputs.json') | from_json | json_query('acr_admin_username.value') }}"
# ACR Password - Desde terraform output acr_password
acr_password: "{{ lookup('file', '../terraform/outputs.json') | from_json | json_query('acr_admin_password.value') }}"
# ACR Login Server - Desde terraform output acr_login_server
acr_login_server: "{{ lookup('file', '../terraform/outputs.json') | from_json | json_query('acr_login_server.value') }}"

# Nombre del cluster AKS - Desde terraform output aks_cluster_name
aks_cluster_name: "{{ lookup('file', '../terraform/outputs.json') | from_json | json_query('aks_cluster_name.value') }}"

#Nombre del grupo de recursos AKS - Desde terraform output resource_group_name
resource_group: "{{ lookup('file', '../terraform/outputs.json') | from_json | json_query('resource_group_name.value') }}"

# Ruta al archivo kubeconfig de AKS - Desde terraform output aks_kube_config
# aks_kube_config: "{{ lookup('file', '../terraform/outputs.json') | from_json | json_query('aks_kube_config.value') }}"

# Configuración de autenticación para ACR
# Este valor se usa para crear un secreto en Kubernetes que permite pull de imágenes desde ACR
# Se genera automáticamente al hacer login en ACR con podman/docker
acr_full_config_json: '{"auths": {"acrcasopractico2amnm6p.azurecr.io": {"auth": "YWNyY2Fzb3ByYWN0aWNvMmFtbm02cDpVcW1ZbjRwVEtiTHI1aXJzVk1JWktaNHRhV3BVd1NXcHFFbkxWMGRSSzgrQUNSQVByOWtp"}}}'